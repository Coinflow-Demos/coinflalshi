# Prisma CLI (generate/push/seed) reads .env from this package's directory.
# Copy this to .env and fill in your Neon connection strings — same values
# as apps/web/.env.local.
DATABASE_URL="postgresql://user:password@ep-example-pooler.neon.tech/coinflalshi?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:password@ep-example.neon.tech/coinflalshi?sslmode=require"
