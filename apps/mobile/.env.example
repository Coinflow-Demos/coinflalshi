# Base URL of the deployed (or locally running) coinflalshi web app — the
# mobile app calls its API routes instead of talking to Postgres/Coinflow directly.
EXPO_PUBLIC_API_BASE_URL="http://localhost:3000"
